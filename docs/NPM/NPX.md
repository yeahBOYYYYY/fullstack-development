# NPX

NPX (Node Package Execute) is a command-line tool bundled with [[NPM 101|NPM]] that lets you execute [[JavaScript 101|JavaScript]] packages directly from the [[NPM 101#Where are the libraries coming from?|npm registry]] without needing to install them globally first. It simplifies running one-off commands and scaffolding projects.
